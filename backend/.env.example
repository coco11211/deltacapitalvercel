# Email Sending Configuration
# Get credentials from your email provider (SendGrid, AWS SES, etc.)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-password

# Optional: From address for emails
EMAIL_FROM=updates@deltacapitaltrading.com

# Newsletter Settings
NEWSLETTER_FROM_NAME=Delta Capital Updates